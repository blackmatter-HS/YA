<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NOOR | Exclusive 18+ Content</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Poppins:wght@300;500&family=Amiri&display=swap" rel="stylesheet">
    <style>
        :root {
            --gold: #D4AF37;
            --dark-gold: #996515;
            --light-gold: #F5DEB3;
            --black: #121212;
            --ivory: #FFFFF0;
            --ruby: #E0115F;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background: url('https://i.imgur.com/PERSIAN_BG.jpg') center/cover fixed;
            color: var(--ivory);
            overflow-x: hidden;
        }
        
        body::before {
            content: "";
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(10, 5, 0, 0.85);
            backdrop-filter: blur(12px) brightness(0.7);
            z-index: -2;
        }
        
        .persian-pattern {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://i.imgur.com/PERSIAN_PATTERN.png');
            opacity: 0.15;
            z-index: -1;
            pointer-events: none;
        }
        
        .age-gate {
            background: linear-gradient(135deg, rgba(30, 15, 5, 0.95) 0%, rgba(50, 25, 10, 0.98) 100%);
            max-width: 650px;
            margin: 10vh auto;
            padding: 50px;
            border-radius: 20px;
            border: 3px solid var(--gold);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.6);
            text-align: center;
            animation: fadeIn 1.5s ease;
            position: relative;
            overflow: hidden;
        }
        
        .age-gate::after {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(212, 175, 55, 0.08) 0%, transparent 70%);
            animation: rotate 20s linear infinite;
            z-index: -1;
        }
        
        .content {
            display: none;
            padding: 50px;
            background: linear-gradient(135deg, rgba(30, 15, 5, 0.95) 0%, rgba(50, 25, 10, 0.98) 100%);
            margin: 5vh auto;
            max-width: 1000px;
            border-radius: 20px;
            border: 3px solid var(--gold);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.6);
            position: relative;
            overflow: hidden;
        }
        
        .content::after {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(212, 175, 55, 0.05) 0%, transparent 70%);
            animation: rotate 20s linear infinite reverse;
            z-index: -1;
        }
        
        h1, h2, h3 {
            font-family: 'Playfair Display', serif;
            color: var(--gold);
            text-shadow: 0 2px 8px rgba(0, 0, 0, 0.7);
            letter-spacing: 1px;
        }
        
        h1 {
            font-size: 3.5rem;
            margin-bottom: 25px;
            letter-spacing: 3px;
            position: relative;
            display: inline-block;
        }
        
        h1::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 0;
            width: 100%;
            height: 2px;
            background: linear-gradient(to right, transparent, var(--gold), transparent);
        }
        
        h2 {
            font-size: 2.2rem;
            margin: 30px 0 20px;
        }
        
        .warning {
            color: var(--light-gold);
            font-weight: 500;
            font-size: 1.3rem;
            margin: 30px 0;
            text-transform: uppercase;
            letter-spacing: 2px;
            font-family: 'Amiri', serif;
        }
        
        .btn {
            background: linear-gradient(45deg, var(--dark-gold) 0%, var(--gold) 50%, var(--light-gold) 100%);
            color: var(--black);
            border: none;
            padding: 18px 45px;
            margin: 20px;
            border-radius: 50px;
            cursor: pointer;
            font-size: 1.2rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            transition: all 0.4s ease;
            box-shadow: 0 6px 20px rgba(212, 175, 55, 0.4);
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(212, 175, 55, 0.5);
            font-family: 'Poppins', sans-serif;
        }
        
        .btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(212, 175, 55, 0.6);
        }
        
        .btn:active {
            transform: translateY(2px);
        }
        
        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: 0.5s;
        }
        
        .btn:hover::before {
            left: 100%;
        }
        
        .exit-btn {
            background: rgba(255, 255, 255, 0.1);
            color: var(--ivory);
            border: 1px solid var(--ruby);
            box-shadow: 0 4px 15px rgba(224, 17, 95, 0.3);
        }
        
        .exit-btn:hover {
            background: rgba(224, 17, 95, 0.2);
            box-shadow: 0 6px 20px rgba(224, 17, 95, 0.4);
        }
        
        .product {
            background: rgba(40, 20, 10, 0.7);
            margin: 40px auto;
            padding: 40px;
            border-radius: 15px;
            width: 85%;
            border: 1px solid rgba(212, 175, 55, 0.2);
            transition: all 0.5s ease;
            position: relative;
            overflow: hidden;
        }
        
        .product::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, transparent 65%, rgba(212, 175, 55, 0.03) 100%);
            pointer-events: none;
        }
        
        .product:hover {
            transform: translateY(-10px);
            border-color: var(--gold);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.4);
        }
        
        .price {
            font-size: 3rem;
            color: var(--gold);
            font-weight: 700;
            margin: 30px 0;
            font-family: 'Playfair Display', serif;
            position: relative;
            display: inline-block;
        }
        
        .price::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 100%;
            height: 1px;
            background: linear-gradient(to right, transparent, var(--gold), transparent);
        }
        
        .payment-info {
            background: rgba(50, 25, 10, 0.9);
            padding: 40px;
            border-radius: 15px;
            margin-top: 40px;
            border-left: 4px solid var(--gold);
            display: none;
            animation: fadeIn 1s ease;
        }
        
        .payment-method {
            background: rgba(60, 30, 15, 0.7);
            padding: 25px;
            border-radius: 10px;
            margin: 25px 0;
            display: flex;
            align-items: center;
            border: 1px solid rgba(212, 175, 55, 0.1);
            transition: all 0.3s ease;
        }
        
        .payment-method:hover {
            border-color: var(--gold);
            background: rgba(70, 35, 15, 0.8);
        }
        
        .payment-method i {
            font-size: 2.5rem;
            color: var(--gold);
            margin-right: 25px;
            min-width: 50px;
            text-align: center;
        }
        
        .payment-details {
            font-family: 'Poppins', sans-serif;
        }
        
        .payment-details strong {
            color: var(--light-gold);
            font-size: 1.1rem;
            display: block;
            margin-bottom: 5px;
            letter-spacing: 1px;
        }
        
        .payment-details code {
            font-family: monospace;
            background: rgba(0, 0, 0, 0.3);
            padding: 5px 10px;
            border-radius: 3px;
            color: var(--ivory);
            font-size: 1rem;
            letter-spacing: 1px;
        }
        
        .exclusive-badge {
            position: absolute;
            top: 20px;
            right: 20px;
            background: var(--ruby);
            color: white;
            padding: 8px 15px;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            box-shadow: 0 3px 10px rgba(224, 17, 95, 0.3);
            transform: rotate(15deg);
        }
        
        .arabic-calligraphy {
            font-family: 'Amiri', serif;
            font-size: 1.5rem;
            color: var(--light-gold);
            margin: 30px 0;
            line-height: 2.5rem;
            text-align: center;
            font-style: italic;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        @keyframes rotate {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        
        .luxury-divider {
            height: 1px;
            background: linear-gradient(to right, transparent, var(--gold), transparent);
            margin: 40px 0;
            opacity: 0.5;
        }
        
        .membership-benefits {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin: 40px 0;
        }
        
        .benefit-item {
            background: rgba(212, 175, 55, 0.1);
            border: 1px solid rgba(212, 175, 55, 0.2);
            padding: 25px;
            border-radius: 10px;
            width: calc(33% - 20px);
            min-width: 250px;
            transition: all 0.3s ease;
        }
        
        .benefit-item:hover {
            background: rgba(212, 175, 55, 0.15);
            border-color: var(--gold);
            transform: translateY(-5px);
        }
        
        .benefit-item i {
            font-size: 2rem;
            color: var(--gold);
            margin-bottom: 15px;
            display: block;
        }
        
        .benefit-item h3 {
            font-family: 'Poppins', sans-serif;
            font-size: 1.2rem;
            margin: 0 0 10px;
            color: var(--light-gold);
        }
        
        .vip-access {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background: var(--gold);
            color: var(--black);
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            box-shadow: 0 5px 15px rgba(212, 175, 55, 0.5);
            cursor: pointer;
            z-index: 100;
            transition: all 0.3s ease;
        }
        
        .vip-access:hover {
            transform: scale(1.1) rotate(15deg);
            box-shadow: 0 8px 20px rgba(212, 175, 55, 0.7);
        }
        
        /* Responsive adjustments */
        @media (max-width: 768px) {
            .age-gate, .content {
                padding: 30px;
                margin: 5vh auto;
                width: 90%;
            }
            
            h1 {
                font-size: 2.5rem;
            }
            
            .benefit-item {
                width: 100%;
            }
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <div class="persian-pattern"></div>

    <!-- Age Verification Gate -->
    <div class="age-gate" id="ageGate">
        <h1>NOOR</h1>
        <div class="arabic-calligraphy">نور الحضور الفاخر</div>
        <div class="luxury-divider"></div>
        <p class="warning">Exclusive 18+ Premium Content</p>
        <p>By entering, you confirm you are at least 18 years old and agree to our terms of service</p>
        <button class="btn" onclick="enterSite()">Enter Sanctuary</button>
        <button class="btn exit-btn" onclick="leaveSite()">I'm Under 18</button>
    </div>

    <!-- Main Content -->
    <div class="content" id="mainContent">
        <h1>THE NOOR EXPERIENCE</h1>
        <div class="arabic-calligraphy">حيث يصبح الرفاهية واقعًا</div>
        <div class="luxury-divider"></div>
        <p>An exclusive collection of the most exquisite digital experiences, curated for discerning individuals</p>
        
        <div class="product">
            <div class="exclusive-badge">Limited Edition</div>
            <h2><i class="fas fa-gem"></i> Royal Collection</h2>
            <p>The ultimate package for those who demand nothing but the finest. Includes:</p>
            <ul>
                <li>100+ ultra-premium AI-generated experiences</li>
                <li>Custom content creation (10 requests/month)</li>
                <li>24/7 VIP concierge service</li>
                <li>Exclusive member-only events</li>
                <li>Priority access to new releases</li>
            </ul>
            
            <div class="membership-benefits">
                <div class="benefit-item">
                    <i class="fas fa-lock"></i>
                    <h3>Discreet Access</h3>
                    <p>Complete privacy with our military-grade encryption</p>
                </div>
                <div class="benefit-item">
                    <i class="fas fa-star"></i>
                    <h3>Premium Quality</h3>
                    <p>Only the highest resolution 8K content</p>
                </div>
                <div class="benefit-item">
                    <i class="fas fa-user-shield"></i>
                    <h3>Dedicated Support</h3>
                    <p>Personal assistant available at all times</p>
                </div>
            </div>
            
            <div class="price">$999<span style="font-size: 1.2rem; color: var(--light-gold);">/month</span></div>
            <button class="btn" onclick="buyProduct()">Begin Your Journey</button>
        </div>

        <div class="payment-info" id="paymentInfo">
            <h2><i class="fas fa-university"></i> Secure Acquisition</h2>
            <p>To maintain exclusivity, we accept only the most discreet payment methods:</p>
            
            <div class="payment-method">
                <i class="fab fa-bitcoin"></i>
                <div class="payment-details">
                    <strong>Crypto Preferred</strong>
                    <code>bc1qxy2kgdygjrsqtzq2n0yrf2493w83kfj00xq4vl</code>
                    <p>10% discount for cryptocurrency payments</p>
                </div>
            </div>
            
            <div class="payment-method">
                <i class="fas fa-swift"></i>
                <div class="payment-details">
                    <strong>International Wire</strong>
                    <code>NOORHOLDINGSCY</code>
                    <p>SWIFT: BNPAPMCLXXX</p>
                    <p>Account: 8800770055</p>
                </div>
            </div>
            
            <div class="payment-method">
                <i class="fab fa-cc-visa"></i>
                <div class="payment-details">
                    <strong>Visa Infinite</strong>
                    <code>4532 7589 1245 7896</code>
                    <p>Name: NOOR PRIVATE CLUB</p>
                    <p>Exp: 12/26 | CVV: ***</p>
                </div>
            </div>
            
            <p style="margin-top: 30px; text-align: center;">
                <i class="fas fa-envelope"></i> Confirmation required: <strong>concierge@noor-vip.com</strong><br>
                Your private access will be activated within 12 hours of verification.
            </p>
        </div>
    </div>

    <div class="vip-access" onclick="scrollToPayment()">
        <i class="fas fa-crown"></i>
    </div>

    <script>
        // Age verification functions
        function enterSite() {
            document.getElementById("ageGate").style.display = "none";
            document.getElementById("mainContent").style.display = "block";
            localStorage.setItem("ageVerified", "true");
            
            // Animate content entrance
            document.getElementById("mainContent").style.animation = "fadeIn 1.5s ease";
        }

        function leaveSite() {
            window.location.href = "https://www.google.com";
        }

        // Purchase function
        function buyProduct() {
            const paymentInfo = document.getElementById("paymentInfo");
            paymentInfo.style.display = "block";
            paymentInfo.scrollIntoView({ behavior: 'smooth' });
            
            // Animate the payment info
            paymentInfo.style.animation = "fadeIn 1s ease";
        }

        // VIP scroll function
        function scrollToPayment() {
            const paymentInfo = document.getElementById("paymentInfo");
            paymentInfo.style.display = "block";
            paymentInfo.scrollIntoView({ behavior: 'smooth' });
        }

        // Check if already verified
        if (localStorage.getItem("ageVerified") === "true") {
            document.getElementById("ageGate").style.display = "none";
            document.getElementById("mainContent").style.display = "block";
        }

        // Disable right-click
        document.addEventListener('contextmenu', function(e) {
            e.preventDefault();
            alert('This content is protected for premium members only');
        });

        // Disable text selection
        document.addEventListener('selectstart', function(e) {
            e.preventDefault();
        });
    </script>
</body>
</html>
